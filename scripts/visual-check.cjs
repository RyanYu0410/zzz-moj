const {chromium}=require(process.env.PLAYWRIGHT_MODULE||'playwright');
const assert=require('node:assert/strict');
(async()=>{const browser=await chromium.launch({headless:true,channel:'chrome'});const page=await browser.newPage({viewport:{width:1280,height:960}});const errors=[];page.on('pageerror',e=>errors.push(e.message));page.on('response',r=>{if(r.url().startsWith('http://localhost')&&r.status()>=400)errors.push(r.url())});await page.goto('http://localhost:8080/');await page.locator('#tile-gallery').click();await page.locator('#tile-catalog .tile').last().waitFor();assert.equal(await page.locator('#tile-catalog .tile').count(),37);await page.screenshot({path:'/tmp/zzz-gallery.png'});await page.locator('#close').click();await page.locator('#preview-chi').click();await page.waitForTimeout(350);assert.equal(await page.locator('#call-fx img').evaluate(e=>e.complete&&e.naturalWidth>0),true);await page.screenshot({path:'/tmp/zzz-chi.png'});await page.waitForTimeout(1400);assert.equal(await page.locator('#call-fx').isVisible(),false);await page.locator('#preview-pon').click();await page.waitForTimeout(350);assert.equal(await page.locator('#call-fx img').evaluate(e=>e.complete&&e.naturalWidth>0),true);await page.waitForTimeout(1400);await page.locator('#hand button').first().dblclick();for(let i=0;i<35;i++){if(await page.locator('#call-panel').isVisible())await page.locator('#call-pass').click();if(await page.locator('#river3 .tile').count())break;await page.waitForTimeout(200)}assert.equal(await page.locator('#river0 .tile').count(),1);assert.equal(await page.locator('#river3 .tile').count(),1);await page.setViewportSize({width:390,height:844});await page.screenshot({path:'/tmp/zzz-mobile.png'});assert(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth));await page.emulateMedia({reducedMotion:'reduce'});await page.locator('#preview-chi').click();assert.equal(await page.locator('#call-fx img').evaluate(e=>getComputedStyle(e).animationName),'none');assert.deepEqual(errors,[]);await browser.close();console.log('PASS: gallery, both PNG previews, actual four-seat discard cycle, mobile overflow, reduced motion, no browser errors')})().catch(e=>{console.error(e);process.exit(1)});
